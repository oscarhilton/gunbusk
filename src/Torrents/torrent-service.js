// import WebTorrent from 'webtorrent';

export default class TorrentService {
	// client = new WebTorrent();

	seedBuffer(buffer, cb) {
		// this.client.seed(buffer, cb);
    console.log(this.client)
	}
}
